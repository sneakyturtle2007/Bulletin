project('Bulletin', 'c')

libdir = 'src/lib'
apidir = 'src/api'
backenddir = 'src/backend'
databasedir = 'src/database'
externaldir = 'external'

subdir(libdir)
subdir(apidir)
subdir(backenddir)
subdir(databasedir)
# If you want to manage external dependencies with Meson, add: subdir(externaldir)